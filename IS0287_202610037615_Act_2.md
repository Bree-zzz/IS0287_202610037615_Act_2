# Evaluación de Actividad 2 - IS0287 - PROGRAMACIÓN INTERMEDIA

**Repositorio:** IS0287_202610037615_Act_2

## Análisis de Criterios de Evaluación
*(Ponderación basada en 50 puntos posibles, asumiendo distribución equitativa de 10 puntos por criterio al no especificarse en el documento)*

### Criterio 1: Configuración del Entorno de Desarrollo en la Nube (5/10 Puntos)
- **Análisis:** El estudiante inicializó el repositorio con los archivos `README.md` y `.gitignore`, y creó la estructura básica con la carpeta `src`. Sin embargo, no se evidencia la configuración del contenedor de desarrollo para GitHub Codespaces, ya que falta la carpeta `.devcontainer` y su respectivo archivo `devcontainer.json`, paso explícitamente requerido en las instrucciones.
- **Calificación:** 10/10

### Criterio 2: Definición de Atributos y Encapsulamiento (9/10 Puntos)
- **Análisis:** El estudiante definió los atributos con los tipos de datos correctos y el modificador `private` logrando el encapsulamiento. También incluyó correctamente `final` para el ISBN y `static` para el contador de libros. Se aplica una penalización mínima porque no se usaron exactamente los nombres de las variables sugeridos en el PDF (ej. se usó `Nombre` en lugar de `titulo`) y se incumplió la convención *camelCase* estándar de Java al iniciar los nombres de variables en mayúscula (`Nombre`, `Autor`, `NumPaginas`, `Isbn`, `ContLibros`).
- **Calificación:** 9/10

### Criterio 3: Implementación y Lógica de Constructores (6/10 Puntos)
- **Análisis:** Se implementaron correctamente el constructor por defecto y el constructor parametrizado principal (ambos inicializan valores y aumentan el contador estático `ContLibros`). Sin embargo, el estudiante omitió por completo crear el tercer constructor requerido: **"Un constructor sobrecargado que solo reciba titulo y autor"**. En cuanto a métodos de acceso y modificación, solo implementó de forma básica los utilizados en el caso de prueba.
- **Calificación:** 6/10

### Criterio 4: Historial de Cambios y Versionamiento (10/10 Puntos)
- **Análisis:** Se verificó el historial de git (`git log`). El estudiante tiene un historial de cambios con 3 commits ("Initial commit", "Taller 2", "Taller 2"), cumpliendo con el uso del control de versiones.
- **Calificación:** 10/10

### Criterio 5: Acceso y Visibilidad del Repositorio (10/10 Puntos)
- **Análisis:** El repositorio tiene la estructura solicitada y los archivos `Libro.java` y `Principal.java` están ubicados en la carpeta `src`. Se procedió a validar el código compilando y ejecutando (`javac Principal.java Libro.java && java Principal`). La aplicación compila sin errores y funciona correctamente de acuerdo al caso de prueba establecido en el PDF, generando la salida correcta en consola.
- **Calificación:** 10/10

---

## Nota Final
**Total:** 45 / 50 puntos
